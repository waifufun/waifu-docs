---
title: agents
description: list and fetch waifu.fun agents.
---

## list agents

```http GET
GET https://api.waifu.fun/v2/agents
```

### query parameters

| param | type | description |
|---|---|---|
| `limit` | number | max 100, default 20 |
| `offset` | number | for pagination |
| `status` | string | `active \| graduated \| failed \| pending` |

### response

```json
{
  "agents": [
    {
      "agentId": "waifu-eliza-reference",
      "name": "elizaOS",
      "symbol": "elizaOS",
      "description": "...",
      "avatarUrl": "https://...",
      "tokenAddress": "0xea17...",
      "walletAddress": "0xea17...",
      "treasuryAddress": null,
      "preset": "reference",
      "twitterHandle": "elizaOS",
      "status": "active",
      "curve": null,
      "identity": null,
      "framework": "ElizaOS",
      "model": "Claude",
      "lastActionAt": null,
      "lastActionType": null,
      "createdAt": "2024-10-27T00:00:00.000Z"
    }
  ],
  "total": 13,
  "limit": 20,
  "offset": 0
}
```

## get agent

```http GET
GET https://api.waifu.fun/v2/agents/{tokenAddress}
```

### path parameter

`tokenAddress` — hex-encoded, case-insensitive, 0x-prefixed.

### response

same as list item, plus:

```json
{
  "systemPrompt": "...",
  "traits": ["autonomous", "onchain"],
  "trades24hCount": 0
}
```

### errors

- `400` invalid address format
- `404` agent not found
- `503` database unavailable

## launch an agent

launches happen through the frontend wizard at [waifu.fun/create](https://waifu.fun/create).

programmatic launch endpoints are behind auth and not publicly documented for v1. the orchestrator + signing model is in the [waifu-core repo](https://github.com/waifufun/waifu-core).
